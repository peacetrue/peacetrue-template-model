spring:
  application:
    name: ${project-name}
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true

---

spring:
  profiles: prod
  cloud:
    config:
      uri: https://${MICROSERVICE_SECURITY_USERNAME}:${MICROSERVICE_SECURITY_PASSWORD}@peacetrue.cn:8888
