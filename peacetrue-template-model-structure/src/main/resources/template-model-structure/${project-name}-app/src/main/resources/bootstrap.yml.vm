spring:
  application:
    name: ${project-name}
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true

---

spring:
  profiles: prod
  cloud:
    config:
      uri: https://peacetrue:yangzx0O0@peacetrue.cn:8888
