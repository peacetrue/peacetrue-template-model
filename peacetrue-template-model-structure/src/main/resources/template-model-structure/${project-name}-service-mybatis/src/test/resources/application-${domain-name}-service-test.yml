spring:
  datasource:
    platform: h2
    schema: classpath:schema-h2.sql
    initialization-mode: embedded
  jpa:
    generate-ddl: true
logging:
  level:
    com:
      github:
        peacetrue: debug
