export const ${moduleName}Messages =  {
    resources: {
        "${module-name}s": {
            name: '${nationalName}',
            fields: {
                #foreach($property in $properties)
                '$property.name': '$property.nationalName',
                #end
            },
        }
    }
}

//[#foreach($property in $properties)'$property.name',#end]
//[#foreach($property in $properties)'$property.nationalName',#end]

export default ${moduleName}Messages;
